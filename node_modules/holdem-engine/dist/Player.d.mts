import Card from './Card.mjs';

declare const PlayerAction: {
    readonly Fold: "Fold";
    readonly Call: "Call";
    readonly Raise: "Raise";
    readonly Check: "Check";
    readonly Bet: "Bet";
    readonly Show: "Show";
    readonly Hide: "Hide";
};
type PlayerAction = (typeof PlayerAction)[keyof typeof PlayerAction];
declare class Player {
    constructor(id: string, name: string, chips: number, hand?: Card[], isFolded?: boolean, betAmount?: number);
    readonly id: string;
    name: string;
    chips: number;
    hand: Card[];
    isFolded: boolean;
    betAmount: number;
    seatNumber: number;
    showCards: boolean;
    updateChips(amount: number): void;
    addCard(card: Card): void;
    /**
     * set seat number
     * @param seatNumber {number}
     */
    setSeat(seatNumber: number): void;
    setBetAmount(amount: number): void;
    getHandEmojiString(): string[];
}

export { PlayerAction, Player as default };
